<?xml version="1.0" encoding="UTF-8"?>
<project name="Git Build" default="push-remote" basedir="..">

	<property file="ant/.toolkit-properties" />


	<macrodef name="git">
		<attribute name="command" />
		<attribute name="dir" default="" />
		<element name="args" optional="true" />
		<sequential>
			<echo message="git @{command} " />
			<exec executable="${GIT_HOME}/git" dir="@{dir}">
				<arg value="@{command}" />
				<args />
			</exec>
		</sequential>
	</macrodef>

	<macrodef name="git-push">
		<attribute name="path" />
		<attribute name="branch" />
		<attribute name="head" />
		<sequential>
			<git command="push" dir="@{path}">
				<args>
					<arg value="@{branch}" />
					<arg value="@{head}" />
				</args>
			</git>
		</sequential>
	</macrodef>

	<target name="commit">

		<condition property="tag.release">
			<matches pattern="([0-9][0-9][0-9]0)" string="${build.int}" />
		</condition>

		<echo message="[Auto Build] Commit Toolkit Release ${version}${build.int}" />

		<git command="add" dir="${basedir}">
			<args>
				<arg value="*.*" />
			</args>
		</git>

		<git command="commit" dir="${basedir}">
			<args>
				<arg value="." />
				<arg value="-m" />
				<arg value="[Auto Build] Toolkit ${version}${build.int}" />
			</args>
		</git>
	</target>

	<target name="tag" if="tag.release" depends="commit">
		<echo message="[Auto Build ] Tag Toolkit Release ${version}${build.int}] " />
		<git command="tag" dir="${basedir}">
			<args>
				<arg value="${version}${build.int}" />
				<arg value="-m" />
				<arg value="[Auto Tag Toolkit Release ${version}${build.int}]" />
			</args>
		</git>
	</target>

	<target name="push-remote" depends="tag">
		<echo message="Pushing to remote repository" />
		<git-push path="${basedir}" branch="origin" head="master" />
	</target>
</project>
