<?xml version="1.0" encoding="UTF-8"?>
<project name="Git Build" default="push-remote" basedir="..">

	<property file="ant/.toolkit-properties" />
	

	<macrodef name="git">
		<attribute name="command" />
		<attribute name="dir" default="" />
		<element name="args" optional="true" />
		<sequential>
			<echo message="git @{command}" />
			<exec executable="git" dir="@{dir}">
				<arg value="@{command}" />
				<args />
			</exec>
		</sequential>
	</macrodef>

	<target name="commit">
		<!--Test for tag -->
	    <condition property="tag.release">
	    	<equals arg1="${build.int}" arg2="0172" />
	    </condition>
		<echo message="[Auto Build] Commit Toolkit Release ${version}${build.int} ${tag.release} ${build.int}"/>
		<!--
		<git command="commit" dir="${basedir}">
			<args>
				<arg value="m" />
				<arg value="[Auto Build Toolkit ${version}${build.int}]" />
			</args>
		</git>
		-->

	</target>
	
	<target name="tag" if="${tag.release}" depends="commit">
	    <echo message="[Auto Build ] Tag Toolkit Release ${version}${build.int}] "/>
		<!--
			<git command="tag" dir="${basedir}">
				<args>
					<arg value="m" />
					<arg value="[Auto Tag Toolkit Release ${version}${build.int}]" />
				</args>
			</git>
		-->		
	</target>
	
	
	<target name="push-remote" depends="tag">
		<echo message="Pushing to remote repository" />
	</target>
</project>
